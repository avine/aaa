import { SidenavNavDirective } from './sidenav.directive';

describe('SidenavDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavNavDirective();
    expect(directive).toBeTruthy();
  });
});
